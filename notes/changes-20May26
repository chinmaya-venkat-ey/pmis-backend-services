1. In divisions dropdown the Others are coming in the first. It should be at the last option in the dropdown
2. The Search User and Search Organization is not getting sorted as the latest one at the top
3. The Users, Organization and Projects; the old database has not been migrated
4. The Division/Owner column is taking division code, not division label in Search User and Search Project pages
5. The Organization column is coming blank in Search User
6. Cannot create dependencies for anything, even after giving different dates. The popup is also not correct.
7. The Priority column in Milestone Config page table is not showing 'P1, P2 or P3'. It is showing 'High, Medium or Low'
8. After publishing, it should redirect user to the Manage Team page to add the users, not the milestone config page
9. The Project Admin is coming in the dropdown in Organization Management page under Project Member
10. Cannot Add users in the Project Admin or Project Member. After adding, if we Save it, they get removed
11. The Audit logs are completely blank. No Audit Logs are being displayed
12. The Master Data tables are not coming updated. There are different data coming in Master Data and Org, User Managements
13. In Critical Path, the flow diagram should be after 'Step 2 · Critical Path Results' and before 'How the Critical Path Is Calculated'
14. In Critical Path, in the critical path boxes in the flow diagram, the delays are not getting calculated properly
15. The Organization admin and Project Admin Roles cannot view anything in the User and Org Managements
16. In the Profile section, the org, mobile and other details are not getting updated
17. In PMIS-user-management/app/controllers/auth_controller.py:66-68, replace UserResponse.model_validate(user) with a call to UserController._build_user_response(user, db) — this routes /me through the same RBAC-recompute path used by GET /users/{id}, so is_admin / is_super_admin reflect live role-assignment state instead of stale ORM columns.
18. The org_role: "admin" text label means nothing — it's just decoration. "Authorization is still driven entirely by role assignments (this column grants nothing)." Either remove org_role from the user model (since it doesn't drive anything), or auto-derive it from the user's highest role assignment so the label always reflects reality. Document clearly in Swagger: passing org_role on create does NOT grant any role. To grant a role you MUST call /role-assignments separately.
19. Latest user on top -- sorting required.
20. I have assigned a project to a user but it is not coming in the response. And vendor_name is not coming in response against the vendor_id in response.
