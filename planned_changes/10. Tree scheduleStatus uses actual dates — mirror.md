# Doc 10 — Tree `scheduleStatus` uses actual dates (mirror)

> **Status: SHIPPED-PENDING-DEPLOY**. Mirror of
> [monolith doc 52](../../PMIS-OpenProject/planned_changes/52.%20Tree%20scheduleStatus%20uses%20actual%20dates%20when%20available.md)
> — the cross-cutting plan lives there.

Mirrors the actual-first / planned-fallback rule into this service's
tree endpoint (`/api/v3/projects/{id}/tree`) so the two services
keep emitting identical `scheduleStatus` / `daysDelayed` for the
same node.

## What landed

- `app/api/v3/tree/service.py:_schedule_status` and
  `_attach_schedule_status` accept two new optional kwargs:
  `actual_start_date`, `actual_end_date`.
- Effective dates: `actual_X or planned_X` independently.
- All four call sites in `build_project_tree` updated.

No DB / wire / FE change. Same behaviour and tests as monolith.

## Outcome

| Commit | What |
|---|---|
| (pending) | tree/service.py mirror of monolith change |
