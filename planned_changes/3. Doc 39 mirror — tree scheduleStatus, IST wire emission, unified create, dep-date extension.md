> **Retrospective doc** — back-filled on 2026-05-12.

# Doc 3 — Doc 39 mirror: tree fields + IST wire emission + dep-date extension

> **Status: SHIPPED**.

Mirrors [monolith doc 39](../../PMIS-OpenProject/planned_changes/39.%20CORS%20preflight%20and%20proxied-response%20headers%20fix%20for%20doc%2037-38%20cutover.md)-era
work into project-mgmt. Doc 39 in monolith was the CORS-preflight
fix; this repo's "doc 3" bundles several adjacent monolith items
that landed in the same window:

| # | Sub-change | Source |
|---|------------|--------|
| 1 | Tree endpoint emits `scheduleStatus` and `daysDelayed` on M-A-T-S nodes. | mirror |
| 2 | Unified create + IST wire emission (IST-aware datetime serialisation). | mirror |
| 3 | Dep-date rule extension across milestone / activity / task hierarchy. | mirror |
| 4 | `vendorName` in tree response. | mirror |
| 5 | Vendor list shows inactive rows by default. | mirror |
| 6 | Children-completed gate at milestone level + cascaded to A/T/S/nested-S. | mirror |

Each item was a small monolith fix that this repo backported in
lock-step to keep mirror parity.

## Outcome

| Commit | What |
|---|---|
| `82af119` | Doc 39 mirror + tree fields + dep-date rule extension |
| `a7bfc74` | Unified create + IST wire emission + tree scheduleStatus |
| `9b0ab02` | Vendor list shows inactive rows by default |
| `a3323a2` | Milestone children-completed gate + vendorName in tree |
| `a37d000` | Children-completed gate at A/T/S/nested-S levels |
