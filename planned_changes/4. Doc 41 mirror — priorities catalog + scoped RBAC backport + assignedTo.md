> **Retrospective doc** — back-filled on 2026-05-12.

# Doc 4 — Doc 41 mirror: priorities catalog + scoped RBAC backport + assignedTo

> **Status: SHIPPED**.

Mirrors the cross-cutting work captured at [monolith doc 41](../../PMIS-OpenProject/planned_changes/41.%20Scoped%20RBAC%20monolith%20stub%20%E2%80%94%20see%20PMIS-user-management%20doc%2041.md)
plus a doc-41 follow-up into this service.

| # | Sub-change | Source |
|---|------------|--------|
| 1 | Activity priority catalog + `activity.priority` column + Swagger fix. | mirror |
| 2 | Optional `assignedTo` (single user) on tasks / subtasks / nested subtasks. | mirror |
| 3 | Scoped-RBAC infrastructure backport: `user_role_assignments` mirror, RBAC repository, scoped permission helpers — needed so this service can evaluate per-project permissions on its writes. | backport |
| 4 | Eight vendor / project mirror items that came with the scoped-RBAC backport. | backport |

The scoped-RBAC backport was unusually heavy for a mirror service —
RBAC evaluation has to happen on this service when it serves write
endpoints, so the `effective_permissions_by_scope` machinery had to
land here too.

## Outcome

| Commit | What |
|---|---|
| `a6a8cdc` | Doc 41 priorities catalog + activity.priority + Swagger fix |
| `8cc1d3b` | Doc 41 follow-up: optional assignedTo |
| `1db4ef5` | Backport doc-41 scoped-RBAC infrastructure + 8 vendor/project mirror items |
