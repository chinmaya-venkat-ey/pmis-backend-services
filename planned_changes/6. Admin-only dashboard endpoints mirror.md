> **Retrospective doc** — back-filled on 2026-05-12.

# Doc 6 — Admin-only dashboard endpoints mirror

> **Status: SHIPPED**.

Mirrors monolith's admin-only dashboard read endpoints (Summary /
Project View / Organization View) into this service so an admin can
hit either the monolith or this service for the same dashboard data.
Reuses the existing dashboard service layer; only adds the routing
+ permission gates.

## Outcome

| Commit | What |
|---|---|
| `c6c43f2` | Mirror admin-only dashboard endpoints from monolith |
