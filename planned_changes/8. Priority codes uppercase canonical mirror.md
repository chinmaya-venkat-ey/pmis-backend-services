> **Retrospective doc** — back-filled on 2026-05-12.

# Doc 8 — Priority codes uppercase canonical mirror

> **Status: SHIPPED**.

Mirrors monolith commit `4840a7c` — priority codes are now stored
and emitted uppercase (`P1` / `P2` / `P3`), with lowercase still
accepted on input for back-compat. Keeps wire shapes in sync between
the two services so an FE that flips between them doesn't see
casing drift.

## Outcome

| Commit | What |
|---|---|
| `2589a13` | Mirror monolith 4840a7c: uppercase priority codes (P1/P2/P3) |
