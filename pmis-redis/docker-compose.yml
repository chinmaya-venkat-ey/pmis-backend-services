version: "3.8"

services:
  redis:
    image: ritamhudait/pmis-redis:v1
    container_name: pmis-redis
    ports:
      - "6379:6379"
    restart: always
    volumes:
      - redis-data:/data

volumes:
  redis-data:
